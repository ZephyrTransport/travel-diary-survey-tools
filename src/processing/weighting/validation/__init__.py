"""Validation sub-package (checksums, weight checks).

* ``checksums`` — recode null detection and incidence-sum overcount checks
  (pre-balancing).
* ``weight_checks`` — post-balancing sanity checks comparing weighted totals
  against PUMS-derived control targets.
"""
